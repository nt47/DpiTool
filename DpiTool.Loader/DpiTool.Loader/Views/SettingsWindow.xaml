<Window x:Class="DpiTool.Loader.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DpiTool.Loader.Views" 
        mc:Ignorable="d"
        Title="设置" Height="446" Width="420" Closing="Window_Closing">
    <Grid >
        <Label Content="Excluded Processes" HorizontalAlignment="Left" Margin="17,15,0,0" VerticalAlignment="Top" Height="39" Width="178" FontSize="18"/>
        <DataGrid x:Name="DataGrid_Main" Margin="17,63,22,34" ItemsSource="{Binding ProcessNames}" SelectedItem="{Binding SelectedItem}" AutoGenerateColumns="False" CanUserAddRows="False">

            <DataGrid.Columns>
                <DataGridTemplateColumn Header="ProcessName" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBox x:Name="TextBox_ProcessName" Text="{Binding ProcessName,UpdateSourceTrigger=PropertyChanged}">
                                <TextBox.InputBindings>
                                    <KeyBinding Key="Return" Command="{Binding Path=DataContext.PressEnterKey, RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=DataGrid}}" CommandParameter="{Binding ElementName=TextBox_ProcessName}"></KeyBinding>
                                </TextBox.InputBindings>
                            </TextBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="-" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" Command="{Binding Path=DataContext.DelClick, RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=DataGrid}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Add" HorizontalAlignment="Left"  VerticalAlignment="Top" Height="30" Width="78" Command="{Binding AddClick}" Margin="211,20,0,0"/>
        <Button Content="Save" HorizontalAlignment="Left" Margin="317,20,0,0" VerticalAlignment="Top" Height="30" Width="68" Command="{Binding SaveClick}"/>
        <Label Content="自动保存：✅" HorizontalAlignment="Left"  VerticalAlignment="Top" RenderTransformOrigin="-0.311,-0.131"  Margin="19,380,0,0" Width="87"/>
    </Grid>
</Window>
